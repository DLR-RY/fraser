1. Adapt IPs & Ports under: configuration_server/config.xml

2. Create default configuration files for all models
& sh createDefaultConfigFiles.sh

3. Start the simulation and Execute all models
$ sh startSimulation.sh